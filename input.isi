programa

declare a,b,c,d.
escreva("programa Teste").
escreva ("Digite B").
leia (a).
escreva ("Digite B".
leia (b).

se (a<b) entao
{
  c := a + b.
} senao
{
  c := a - b.
}

escreva ("C e igual a ").
escreva (c).

d := c * a + b.

escreva ("D e igual a ").
escreva (d).

fimprog.
